<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.google.s3.notice.NoticeDAO">
	<select id="getTotalCount" resultType="Long" parameterType="Pager">
		select count(num) from notice
		where
		<choose>
			<when test="kind == 'Writer'">
				writer
			</when>
			<when test="kind == 'Contents'">
				contents
			</when>
			<otherwise>
				title
			</otherwise>
		</choose>
		like '%' || #{search} || '%' and num>0
	</select>

	<select id="getList" resultType="NoticeDTO"
		parameterType="Pager">
		select * from
		(select rownum R, N.* from
		(select * from
		notice where
		<choose>
			<when test="kind=='Writer'">
				Writer
			</when>
			<when test="kind=='Contents'">
				Contents
			</when>
			<otherwise>
				Title
			</otherwise>
		</choose>
		like '%' || #{search} || '%' and num>0 order by num desc) N)
		where R
		between #{startRow}
		and #{lastRow}
	</select>

	<select id="getSelect" parameterType="NoticeDTO"
		resultType="NoticeDTO">
		select * from notice where num = #{num}
	</select>

	<insert id="setInsert" parameterType="NoticeDTO">
		insert into notice
		values(notice_seq.nextval, #{title},
		#{writer}, #{contents}, sysdate,
		0)
	</insert>

	<update id="setUpdate" parameterType="NoticeDTO">
		update notice set
		title=#{title}, writer=#{writer},
		contents=#{contents},
		regdate=#{regdate}, hit=#{hit} where num=#{num}
	</update>

	<delete id="setDelete" parameterType="NoticeDTO">
		delete notice where
		num=#{num}
	</delete>
</mapper>